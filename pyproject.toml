[project]
name = "marmitaria"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "httpx>=0.28.1",
    "psycopg2-binary>=2.9",
    "python-dotenv>=1.0",
    "uvicorn>=0.41.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=9.0.2",
]
