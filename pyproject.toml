[project]
name = "marmitaria"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "psycopg2-binary>=2.9",
    "python-dotenv>=1.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=9.0.2",
]
